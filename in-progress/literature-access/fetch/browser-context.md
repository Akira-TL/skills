# 浏览器上下文传输

只有普通 HTTP replay 因会话绑定、动态 token 或浏览器安全上下文失败时使用。

仍然不使用浏览器默认下载目录。要求浏览器能力在已授权上下文中完成资源请求，并把响应体以内联方式交回，例如：

- page-context fetch 后返回 bytes；
- 网络响应 body capture；
- 等价的内存/临时文件传输接口。

浏览器只提供当前响应体；保存、命名和全文验收仍由本 Skill 完成。
