# Rapid Checks

运行能够证明当前修改正确且没有破坏直接邻接行为的最小检查集合。

优先顺序：

1. 当前切片的单点测试或可重复行为检查。
2. 受影响模块的相关测试。
3. 项目适用的 typecheck / build / lint 中，能够捕捉本次改动错误的检查。
4. 只有风险、项目约束或改动范围要求时才扩大到完整 test suite。

“相关”由修改的真实调用路径和依赖关系决定，不按方便程度挑检查。若发现数据、安全、权限、迁移或广泛 blast radius，验证强度立即上调，不受 Rapid 默认裁剪限制。
