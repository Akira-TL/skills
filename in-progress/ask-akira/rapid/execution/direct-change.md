# Rapid Direct Change

沿用项目已经存在的最窄实现模式完成当前行为，不为“以后可能需要”创建新的抽象、配置层或扩展点。

执行时：

1. 先定位当前行为真正经过的接口和调用路径。
2. 只修改完成当前 Goal 所需的代码。
3. 优先复用已有模块、类型和测试 seam。
4. 修改后立即运行最相关的语法 / 类型 / 单点行为检查，尽早发现方向错误。

如果实现过程中出现复杂行为，切到 `critical-path-testing.md`；如果出现无法解释的 bug，交给 Matt `diagnosing-bugs`。不要让“direct”变成跳过证据。
