# Emergency Risk Review

只审这次补丁可能造成的事故级后果：

- 修改是否超出锁定的 incident scope；
- 是否影响数据完整性、迁移、权限、安全或兼容性；
- 是否存在新的失败模式会让原事故以另一种形式返回；
- 是否遗漏同一根因的直接同类调用路径；
- 是否残留 debug instrumentation、临时开关、秘密信息或绕过保护的代码。

发现普通代码坏味道但不会影响当前恢复时，把它作为后续工作，不扩大 incident patch。发现事故级风险时继续处理并重新验证，直到风险有证据关闭。
