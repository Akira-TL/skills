# Emergency Regression First

如果现有 public seam 能真实复现事故模式，先把最小复现变成一个失败的回归测试，再修改生产代码。

要求：

1. 测试在修复前确实因当前事故症状变红。
2. 测试走真实调用模式，不用过浅单元测试伪造信心。
3. 只断言需要防止当前事故复发的行为，不顺带扩大测试范围。
4. 修复后该测试变绿，并且原始未最小化反馈环也重新变绿。

如果没有正确 seam，记录这一事实并改用 `minimal-patch.md`；不要为了“必须有测试”临时暴露内部实现。
