# Competition Value Slice

一次只实现当前 frontier 中价值最高且能独立验证的切片。

- 优先完整走通一个用户行为，而不是把某一技术层做得很深。
- 实现细节优先沿用项目已有模式；不为比赛中的假想未来需求建立通用框架。
- 复杂关键逻辑必须有可重复证据；低风险展示胶水可以先实现后 smoke，但不能完全不验证。
- 外部依赖不稳定时优先建立清楚的 fallback，而不是把整个 Demo 成败绑定在无法控制的服务上。
- 当前切片能演示后立即集成，不等待其他切片一起完成。
