# Competition End-to-end Skeleton

先让 Demo Critical Path 从入口到核心结果真实连通，再优化任何单层实现。

Skeleton 可以粗糙，但必须：

- 使用真实的主要模块和真实数据流方向，而不是一组无法演化的静态假页面；
- 从用户演示入口能够走到核心结果；
- 对尚未完成的非关键部分使用最简单、明确可替换的临时实现；
- 启动和复现路径足够简单，让父 Agent 能持续 smoke test。

不要在 skeleton 阶段先把数据库、后端、前端各自“做完整”。第一目标是尽早暴露集成失败。
